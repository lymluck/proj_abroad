<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval"
    android:useLevel="false">

    <!-- 实心 -->
    <solid android:color="@color/transparent" />

    <stroke
        android:width="0.5dp"
        android:color="#C4C9CC" />
    <!-- 圆角 -->
    <corners android:radius="360dp" />

    <!-- 大小 -->
    <size
        android:width="17dp"
        android:height="17dp" />

    <padding
        android:bottom="1dp"
        android:left="1dp"
        android:right="1dp"
        android:top="1dp" />

</shape>